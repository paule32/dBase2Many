--- generator_boolean_fix_v2.py
+++ generator_boolean_fix_v3.py
@@ -21283,12 +21283,15 @@
                 ctx.factor()
             )
 
-            if expr_type != "boolean":
+            # Boolean aus System.Types ist ein Subrange 0..1 und wird
+            # intern als Integer gespeichert. Beide Darstellungen sind
+            # deshalb gültige Operanden für NOT.
+            if expr_type not in ("boolean", "integer"):
                 raise CompileError(
                     ctx,
                     "E0005",
                     got=expr_type,
-                    expected="boolean"
+                    expected="boolean/integer"
                 )
 
             true_label = self.new_named_label(
